export default function FormBody({ children }) {
    return (
        <section id="form-body">
            <div className="space-y-4">
                { children }
            </div>
        </section>
    );
}
